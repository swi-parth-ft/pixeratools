# Pixera Growth Operator Run

Date: 2026-04-08
Current focus: expand buyer-entry pages, strengthen internal discovery, and remove weak schema before the next deploy.

## Data Refresh Attempts

- GA4 refresh against property `500585866` failed with `403 PERMISSION_DENIED` and `ACCESS_TOKEN_SCOPE_INSUFFICIENT` when calling the Analytics Data API from the locally authenticated Google account.
- Google Search Console refresh for `https://pixeratools.com/` failed with `403 PERMISSION_DENIED` and `ACCESS_TOKEN_SCOPE_INSUFFICIENT`.
- No Lemon Squeezy API key, purchase webhook sink, or checkout analytics export path was present in the repo or current environment.
- Result: this run could not refresh live impressions, clicks, CTR, `open_guide`, or purchase data. Decisions fell back to site structure, local funnel wiring, internal-link flow, and current competitor-page review.

## Last 3 Report Comparison

- `2026-04-02`: expanded from one indexable page to five and wired the on-site funnel events (`open_guide`, `view_pricing`, `begin_checkout`, `download_installer`, `generate_lead`).
- `2026-04-03`: tightened crawl readiness with AI-crawler directives, `llms.txt`, `llms-full.txt`, descriptive anchors, and a local deep audit score of `73/100`.
- `2026-04-08`: expanded from five to eight indexable URLs, shifted strategy toward buyer-entry pages, and removed `FAQPage` JSON-LD from the commercial site.
- Trailing 14-day trend status: unavailable. GA4 and Search Console refresh were blocked, so there is no fresh source-backed impressions/clicks/CTR trend in this run.
- Stagnation status: not confirmed from live data. Growth work continued instead of waiting on access.

## Keyword And Strategy Notes

- Manual keyword work and page selection in this run prioritized:
  - `documentation screenshots mac`
  - `pixera vs cleanshot x`
  - `pixera vs xnapper`
- Strategy shift:
  - stop treating `FAQPage` schema as an SEO asset on this commercial software site
  - move beyond capability-only pages into use-case and comparison entry points with stronger buyer intent
- Competitor review sources checked on 2026-04-08:
  - [CleanShot X pricing](https://cleanshot.com/pricing)
  - [CleanShot X features](https://cleanshot.com/features)
  - [Xnapper pricing](https://xnapper.com/pricing)

## Changes Implemented

- Added three new indexable pages:
  - `https://pixeratools.com/documentation-screenshots-mac/`
  - `https://pixeratools.com/pixera-vs-cleanshot-x/`
  - `https://pixeratools.com/pixera-vs-xnapper/`
- Expanded the homepage guide hub from four cards to seven so the new docs and comparison pages inherit homepage visibility immediately.
- Updated `analytics-events.js` metadata routing and `GUIDE_PATHS` so clicks into the new pages continue to fire `open_guide`.
- Strengthened internal links from the core guide pages into the new docs and comparison cluster.
- Removed `FAQPage` JSON-LD from the homepage and all guide/comparison pages while keeping the visible FAQ content intact.
- Updated `sitemap.xml`, `llm.txt`, `llms.txt`, and `llms-full.txt` to reflect the larger page inventory.

## Validation

- `xmllint --noout sitemap.xml` passed.
- `robots_checker.py http://127.0.0.1:4180` passed; all targeted AI crawlers are explicitly allowed.
- `llms_txt_checker.py http://127.0.0.1:4180` scored `100/100`.
- `internal_links.py http://127.0.0.1:4180 --depth 1 --max-pages 40 --json` found:
  - `65` internal links
  - no orphan candidates
  - `8` incoming links for `/documentation-screenshots-mac/`
  - `6` incoming links for `/pixera-vs-cleanshot-x/`
  - `5` incoming links for `/pixera-vs-xnapper/`
- `broken_links.py http://127.0.0.1:4180 --workers 5` found `0` broken links and `1` redirect.
- `social_meta.py` scored `92/100` for the homepage and the new pages after social meta cleanup.
- `generate_report.py http://127.0.0.1:4180 --output reports/seo/SEO-REPORT-2026-04-08.html` saved an HTML audit dashboard and returned an overall score of `72/100`.

## Funnel Status

- On-site funnel events remain wired for:
  - `open_guide`
  - `view_pricing`
  - `begin_checkout`
  - `download_installer`
  - `generate_lead`
- The new docs and comparison pages are included in `GUIDE_PATHS`, so homepage and related-page clicks into them will continue to register as `open_guide`.
- Purchase attribution remains unresolved because checkout completes off-domain and no Lemon Squeezy-side order data is currently available in this workspace.

## Blockers

- GA4 and Search Console access remain blocked by missing OAuth scopes in the current local Google auth session.
- Checkout purchase data is still unavailable locally, so the funnel stops at `begin_checkout` unless Lemon Squeezy data is exposed elsewhere.

## Next Bets

1. Restore GA4 and Search Console OAuth scopes, or provide a service-account/API path, so the next run can compare real 14-day deltas and weak-CTR pages.
2. Deploy this batch and watch which buyer-entry page earns impressions first: docs use case, CleanShot X comparison, or Xnapper comparison.
3. Expand the first page that earns impressions into a sibling cluster page instead of creating another generic landing page.
