# Pixera Growth Operator Run

Date: 2026-04-09
Current focus: extend the buyer-intent comparison cluster, keep homepage discovery strong, and remove the lingering social-meta inconsistency while live analytics access is still blocked.

## Data Refresh Attempts

- GA4 refresh against property `500585866` again failed on `2026-04-09` with `403 PERMISSION_DENIED` and `ACCESS_TOKEN_SCOPE_INSUFFICIENT` when using the locally authenticated Google account token from `gcloud auth print-access-token`.
- Google Search Console refresh for `https://pixeratools.com/` again failed on `2026-04-09` with `403 PERMISSION_DENIED` and `ACCESS_TOKEN_SCOPE_INSUFFICIENT`.
- No Lemon Squeezy API key, purchase webhook sink, or checkout analytics export path was present in the repo or current environment.
- Result: this run still could not refresh live impressions, clicks, CTR, `open_guide`, or purchase data. Decisions again fell back to site structure, public competitor pages, internal-link flow, and local validation.

## Last 3 Report Comparison

- `2026-04-03`: tightened crawl readiness with AI-crawler directives, `llms.txt`, `llms-full.txt`, and descriptive guide anchors.
- `2026-04-08`: shifted the content strategy toward buyer-entry pages by shipping docs and comparison pages, and removed `FAQPage` JSON-LD from the commercial site.
- `2026-04-09`: extended the comparison cluster with `https://pixeratools.com/pixera-vs-shottr/`, expanded the homepage guide hub again, and closed the optional `og:locale` social-meta gap across the public page set.
- Trailing 14-day trend status: unavailable. GA4 and Search Console refresh remained blocked, so there is still no source-backed 14-day trend for impressions, clicks, CTR, or funnel events in this run.
- Stagnation status: live movement is still unconfirmed because reporting access has failed across consecutive runs. Growth work continued instead of waiting, but this is now a repeat blind spot rather than a one-off blocker.

## Keyword And Strategy Notes

- Manual keyword work and page selection in this run prioritized:
  - `pixera vs shottr`
  - `shottr alternative`
  - `mac screenshot app comparison`
- Strategy decision on `2026-04-09`:
  - keep expanding the comparison cluster around real buyer-evaluation queries instead of broad generic landing pages
  - pick competitors that overlap on screenshot utility and price sensitivity, not just styling-first tools
  - keep all comparison claims dated and tied to official vendor pages
- Competitor review sources checked on `2026-04-09`:
  - [Shottr homepage](https://shottr.cc/)
  - [Shottr purchase page](https://shottr.cc/purchase.html)

## Changes Implemented

- Added one new indexable comparison page:
  - `https://pixeratools.com/pixera-vs-shottr/`
- Expanded the homepage guide hub from seven cards to eight so the Shottr comparison inherits direct homepage visibility immediately.
- Updated `analytics-events.js` metadata routing and `GUIDE_PATHS` so the new page participates in `open_guide` discovery flow and metadata stabilization.
- Added a contextual internal link from `https://pixeratools.com/screenshot-backgrounds-mac/` into the new Shottr comparison because Shottr explicitly markets beautiful backgrounds too.
- Updated `sitemap.xml`, `llm.txt`, `llms.txt`, and `llms-full.txt` to reflect the larger comparison inventory.
- Standardized the public page set around explicit social meta:
  - `og:locale`
  - `og:image:width`
  - `og:image:height`
  - `twitter:site`
  - `twitter:creator`

## Validation

- `git diff --check` passed.
- `xmllint --noout sitemap.xml` passed.
- `robots_checker.py http://127.0.0.1:4191` passed; all targeted AI crawlers remain explicitly allowed.
- `llms_txt_checker.py http://127.0.0.1:4191` scored `100/100`.
- `internal_links.py http://127.0.0.1:4191 --depth 1 --max-pages 50 --json` found:
  - `74` internal links
  - no orphan candidates
  - `3` incoming links for `/pixera-vs-shottr/`
  - `9` incoming links for `/screenshot-backgrounds-mac/`
- `broken_links.py http://127.0.0.1:4191 --workers 5` found `0` broken links and `1` redirect.
- `social_meta.py` scored `100/100` for:
  - `http://127.0.0.1:4191`
  - `http://127.0.0.1:4191/pixera-vs-shottr/`

## Funnel Status

- On-site funnel events remain wired for:
  - `open_guide`
  - `view_pricing`
  - `begin_checkout`
  - `download_installer`
  - `generate_lead`
- The new Shottr page is included in `GUIDE_PATHS`, so homepage and related-page clicks into it will register as `open_guide`.
- Purchase attribution remains unresolved because checkout completes off-domain and no Lemon Squeezy-side order data is currently available in this workspace.

## Deep Audit Status

- Deep audit was not triggered on `Thursday, 2026-04-09`.
- Reason:
  - this is not the scheduled Sunday deep-audit run
  - there were no conflicting technical findings in the local validation pass
  - only one comparison page shipped, so the lighter growth iteration was the better fit

## Blockers

- GA4 access remains blocked by missing OAuth scopes in the current local Google auth session.
- Search Console access remains blocked by missing OAuth scopes in the current local Google auth session.
- Checkout purchase data is still unavailable locally, so the funnel stops at `begin_checkout` unless Lemon Squeezy data is exposed elsewhere.

## Next Bets

1. Restore GA4 and Search Console scopes, or provide a service-account/API path, so the next run can judge weak-CTR pages and real 14-day movement instead of making another blind content bet.
2. Watch whether the new comparison branch earns the first impression signal: `pixera-vs-shottr`, `pixera-vs-cleanshot-x`, or `pixera-vs-xnapper`.
3. If comparison pages start earning impressions before the docs branch, expand the comparison cluster again with another truthful competitor page instead of another broad guide.
4. Proposed strategy upgrade for human approval: give this automation an authenticated GA4/Search Console read path, or explicitly narrow the prompt to content-only mode so future runs stop burning cycles on repeated blocked analytics refreshes.
