# Pixera Growth Operator Run

Date: 2026-04-02
Domain: `https://pixeratools.com`
Repo: `/Users/parthantala/Code/pixeratools`

## Audit Summary

- The live site is now sending GA4 events into property `500585866`, and the Realtime API returned `1` active user during verification.
- The site now measures:
  - `view_pricing`
  - `begin_checkout`
  - `download_installer`
  - `generate_lead`
- The site now also measures `open_guide`, which captures clicks from the homepage into the new feature-guide cluster.
- Historical GA data is still sparse, so decisions in this run were based on site structure, live instrumentation checks, and keyword-intent analysis.
- The highest-leverage issue was page inventory: the site had one indexable URL, which severely limited keyword coverage.
- Because this repo is a built export that hydrates into a React app, homepage HTML-only edits needed runtime stabilization to survive hydration.

## Implemented Changes

- Repositioned the homepage metadata around the broader commercial-intent keyword `mac screenshot editor`.
- Added richer `SoftwareApplication` schema and `FAQPage` schema to the homepage.
- Created four new keyword-targeted landing pages:
  - `https://pixeratools.com/mac-screenshot-editor/`
  - `https://pixeratools.com/screenshot-annotation-tool-mac/`
  - `https://pixeratools.com/redact-screenshots-mac/`
  - `https://pixeratools.com/screenshot-backgrounds-mac/`
- Updated `sitemap.xml`, `robots.txt`, and `llm.txt` for discovery.
- Added a homepage guide hub that links into the new landing pages from the strongest root URL.
- Stabilized homepage metadata at runtime so title and description remain correct after hydration.
- Added `open_guide` tracking for the new internal-content funnel.

## Keyword Strategy

- Broad commercial page:
  - `mac screenshot editor`
- Feature-intent pages:
  - `screenshot annotation tool mac`
  - `redact screenshots mac`
  - `screenshot backgrounds mac`
- Supporting variants covered naturally on-page:
  - `annotate screenshots on mac`
  - `blur screenshot background`
  - `redact sensitive information from screenshots`
  - `style screenshots for social media`

## GA4 Status

Measurement ID in site code: `G-8233939FQQ`

Live site instrumentation verified with Playwright:

- `view_pricing`
- `begin_checkout`
- `download_installer`
- `generate_lead`
- `open_guide`

Realtime API snapshot:

- `activeUsers`: `1`

Standard event reporting is not populated enough yet to rank event volume over time.

## Search Console Status

Ready in code:

- `https://pixeratools.com/robots.txt`
- `https://pixeratools.com/sitemap.xml`
- homepage and landing-page canonicals

Still depends on account or DNS access:

1. Add a Domain property for `pixeratools.com`.
2. Verify it with a DNS TXT record.
3. Submit `https://pixeratools.com/sitemap.xml`.
4. Request indexing for the homepage and new landing pages after deployment.

## Revenue Tracking Gap

The site now measures visit -> guide click -> pricing view -> checkout click -> installer download -> lead click, but confirmed `purchase` events still require Lemon Squeezy-side analytics or webhook-based order tracking.

## Next Actions

1. Let Google recrawl the new URLs and watch which landing page earns impressions first.
2. Add Lemon Squeezy purchase tracking to close the attribution loop.
3. Expand the best-performing landing page into a topic cluster with comparison pages, template pages, or audience-specific pages.
