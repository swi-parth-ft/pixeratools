# Pixera SEO And Analytics Baseline

Date: 2026-04-02
Domain: `https://pixeratools.com`
Repo: `/Users/parthantala/Code/pixeratools`

## Shipped In Repo

- Added a canonical homepage URL.
- Added `SoftwareApplication` structured data for Pixera.
- Tightened title, description, Open Graph, and Twitter metadata.
- Added `analytics-events.js` to track high-intent actions.
- Updated `sitemap.xml` to a clean, current one-page sitemap.

## GA4 Status

Measurement ID in site code: `G-JQMBBL1XD8`

Events now emitted by the site:

- `view_pricing`
- `begin_checkout`
- `download_installer`
- `generate_lead`

Recommended GA4 admin actions:

1. Confirm the web data stream URL is `https://pixeratools.com/`.
2. Keep enhanced measurement enabled.
3. Mark `begin_checkout`, `download_installer`, and `generate_lead` as key events.
4. Register event-scoped custom dimensions for:
   - `cta_location`
   - `item_name`
   - `platform`
   - `link_url`
5. Associate Search Console with the same GA4 property.

## Search Console Status

Already ready in code:

- `https://pixeratools.com/robots.txt`
- `https://pixeratools.com/sitemap.xml`
- homepage canonical

Still requires Google account or DNS access:

1. Add a Domain property for `pixeratools.com`.
2. Verify it with a DNS TXT record.
3. Submit `https://pixeratools.com/sitemap.xml`.
4. Inspect the homepage URL and request indexing after deployment.

## Revenue Tracking Gap

The site can now measure visit -> pricing view -> checkout click -> installer download.

Confirmed purchase revenue is still incomplete from this static export alone because checkout finishes off-domain on Lemon Squeezy. To close that gap, use at least one of these:

- Lemon Squeezy's Google Analytics integration for checkout-side purchase events.
- A server-side webhook receiver that records successful orders.
- A custom checkout flow that returns buyers to a tracked success page on `pixeratools.com`.

## Next Actions

1. Deploy these changes.
2. Open Realtime in GA4 and verify all four events.
3. Finish Search Console domain verification and sitemap submission.
4. Review Search Console coverage and GA4 traffic quality after 7 to 14 days.
